# Django in containers using ansible-container

This is a proof of concept rewrite from docker-compose & Dockerfiles to ansible-container playbooks.

At the moment only a very basic scenario is implemented.
